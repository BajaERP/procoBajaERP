services:
  postgres:
    restart: unless-stopped

  backend:
    build:
      target: runtime
    restart: unless-stopped
    ports:
      - "${BACKEND_PORT:-8080}:8080"

  frontend:
    build:
      target: production
    restart: unless-stopped
    ports:
      - "${FRONTEND_PROD_PORT:-80}:80"
